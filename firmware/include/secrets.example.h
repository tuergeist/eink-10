// Copy this file to secrets.h and fill in real values.
// secrets.h is gitignored.
#pragma once

#define WIFI_SSID  "your-ssid"
#define WIFI_PASS  "your-password"

// Bearer token for the server's /config.json + /dashboard.png endpoints.
// Must match $EINK_READ_TOKEN on the server.
#define READ_TOKEN "your-read-token"
