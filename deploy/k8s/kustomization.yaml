apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: eink
resources:
  - namespace.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  # secret.yaml — apply separately or generate via SOPS / sealed-secrets
